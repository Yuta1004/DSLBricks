[package]
name = "test_default"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
anyhow = "1.0"
thiserror = "1.0"
serde_json = "1.0"
compiler = { path = "../../compiler/compiler" }

[build-dependencies]
compiler = { path = "../../compiler/compiler", features = ["build"] }
