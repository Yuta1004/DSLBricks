[package]
name = "lib_runtime_compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
thiserror = { workspace = true }
compiler = { workspace = true, features = ["dev"] }
clap = { version = "4.3", features = ["derive"] }

